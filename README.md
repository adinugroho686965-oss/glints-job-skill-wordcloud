# glints-job-skill-wordcloud
This tool extracts  data from job pages (via cookies + URL), processes skill frequency, and visualizes the results in a zoomable, draggable word cloud.

![Preview](preview.png)

